[build]
  base    = "frontend"
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback — bắt buộc cho Vue Router history mode
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
